<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/list_admin.css">
    <link rel="stylesheet" href="/css/tweaks-panel.css">
    <title>Posts — Admin</title>
</head>
<body>
    <canvas id="bgfx"></canvas>

    <header class="admin-header">
        <a href="/admin" class="btn-voltar">← admin</a>
        <h1>Posts</h1>
        <div class="header-right">
            <button type="button" id="open-tweaks" class="btn-tweaks">⚙</button>
            <a href="/criarPost" class="btn-novo">+ novo post</a>
        </div>
    </header>

    <div class="lista">
        <% if (posts.length === 0) { %>
            <p class="vazio">Nenhum post cadastrado ainda.</p>
        <% } %>
        <% for (let i = 0; i < posts.length; i++) { %>
        <div class="item">
            <span class="item-titulo"><%=posts[i].titulo%></span>
            <div class="item-acoes">
                <a href="/postEditar/<%=posts[i].id%>" class="btn-editar">editar</a>
                <button type="button" data-id="<%=posts[i].id%>" class="btn-deletar">deletar</button>
            </div>
        </div>
        <% } %>
    </div>

    <script src="/js/postAdmin.js"></script>
</body>

<%- include('partials/tweaks-panel') %>
</html>
