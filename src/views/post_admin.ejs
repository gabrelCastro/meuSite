<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="/css/list_admin.css">
    <title>Posts — Admin</title>
</head>
<body>
    <header class="admin-header">
        <a href="/admin" class="btn-voltar">← Admin</a>
        <h1>Posts</h1>
        <a href="/criarPost" class="btn-novo">+ Novo Post</a>
    </header>

    <div class="lista">
        <% if (posts.length === 0) { %>
            <p class="vazio">Nenhum post cadastrado ainda.</p>
        <% } %>
        <% for (let i = 0; i < posts.length; i++) { %>
        <div class="item">
            <span class="item-titulo"><%=posts[i].titulo%></span>
            <div class="item-acoes">
                <a href="/postEditar/<%=posts[i].id%>" class="btn-editar">Editar</a>
                <a href="#" data-id="<%=posts[i].id%>" class="btn-deletar" onclick="deletePost(this,event)">Deletar</a>
            </div>
        </div>
        <% } %>
    </div>

    <script src="/js/postAdmin.js"></script>
</body>
</html>
