<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Vídeo — Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/form_admin.css">
    <link rel="stylesheet" href="/css/tweaks-panel.css">
</head>
<body>
    <canvas id="bgfx"></canvas>

    <header class="admin-header">
        <a href="/videoAdmin" class="btn-voltar">← vídeos</a>
        <h1>Editar Vídeo</h1>
        <button type="button" id="open-tweaks" class="btn-tweaks">⚙</button>
    </header>

    <div class="form-container">
        <% if (error) { %><div class="form-error"><%= error %></div><% } %>
        <form id="updateVideoForm" method="POST" action="/videoEditar/<%=video.id%>" class="formulario" enctype="multipart/form-data">
            <div class="campo">
                <label for="titulo">Título</label>
                <input type="text" id="titulo" name="titulo" class="input" value="<%=video.titulo%>" required>
            </div>

            <div class="campo">
                <label for="imageInput">Imagem de capa <span class="hint">(deixe vazio para manter a atual)</span></label>
                <input name="imagem" type="file" accept="image/*" id="imageInput" class="input-file">
            </div>

            <div class="campo">
                <label for="conteudo">Novo link do vídeo <span class="hint">(deixe vazio para manter o atual)</span></label>
                <input type="url" name="conteudo" id="conteudo" class="input"
                    placeholder="https://www.youtube.com/watch?v=... ou https://vimeo.com/...">
                <span class="hint">Suporta YouTube e Vimeo</span>
            </div>

            <div class="campo">
                <label for="descricao">Descrição curta <span class="hint">(opcional)</span></label>
                <input type="text" id="descricao" name="descricao" class="input"
                    value="<%=video.descricao || ''%>" maxlength="500"
                    placeholder="Breve descrição do vídeo">
            </div>

            <div class="campo">
                <label for="serie">Série <span class="hint">(opcional)</span></label>
                <input type="text" id="serie" name="serie" class="input"
                    value="<%=video.serie || ''%>" placeholder="Ex: Java na prática">
            </div>

            <div class="campo">
                <label for="tags">Tags <span class="hint">(opcional)</span></label>
                <input type="text" id="tags" name="tags" class="input"
                    value="<%=(video.tags || []).join(', ')%>">
                <span class="hint">Separe com vírgula</span>
            </div>

            <div class="campo">
                <label for="corpo">Conteúdo adicional <span class="hint">(opcional)</span></label>
                <textarea name="corpo" id="corpo"><%- video.corpo || '' %></textarea>
            </div>

            <button type="submit" class="btn-submit">Salvar alterações</button>
        </form>
    </div>

    <script src="/tinymce/tinymce.min.js"></script>
    <script src="/js/videoForm.js"></script>
    <script src="/js/video_editar.js"></script>
</body>

<%- include('partials/tweaks-panel') %>
</html>
